<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Fondo del contenedor -->
    <solid android:color="@android:color/white" />



    <!-- Efecto de brillo -->
    <gradient
        android:startColor="@color/icon_nav"
        android:centerColor="@color/contenedores"
        android:endColor="@color/icon_nav"
        android:angle="45" />

    <!-- Trazo para definir los bordes -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" />
</shape>
