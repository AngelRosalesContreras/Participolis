package com.example.participolis

import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.util.Log
import android.widget.ImageView
import android.widget.LinearLayout
import android.widget.ListView
import android.widget.TextView
import android.widget.Toast
import androidx.cardview.widget.CardView

class Foro : AppCompatActivity() {


    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_foro)

        //        cardCultura.setOnClickListener {
        //            Toast.makeText(this,"Se pulsó la card de cultura", Toast.LENGTH_SHORT).show()
        //            val intent = Intent(this, CulruraActivity::class.java)
        //            startActivity(intent)
        //        }
        //
        //        cardSeguridad.setOnClickListener {
        //            Toast.makeText(this,"Se pulsó la card de seguridad", Toast.LENGTH_SHORT).show()
        //
        //        }
    }
}